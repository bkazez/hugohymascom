---
title: Home
css_class: home
---
<section class="bio">
  <div class="container">
    <div class="row">
      <div class="col-sm-8 col-sm-offset-2">
        <%= markdown(File.read("data/bio-short.md")) %>
        <a href="/about" class="read-more-btn">Read More</a> ›
      </div>
    </div>
  </div>
</section>

<section class="videos">
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
        <div class="scroll-row">
          <% data.media.each do |video| %>
		      <div class="video-card">
		        <lite-youtube
		          videoid="<%= video[:media] %>"
		          playlabel="<%= video[:title] %>"
		          posterquality="maxresdefault"
		          params="rel=0"
		          ></lite-youtube>
		        <div class="video-title"><%= video[:title] %></div>

		        <% if video[:credit].present? %>
		          <div class="credit"><%= video[:credit] %></p>
		        <% end %>
		      </div>
          <% end %>
        </div>
        <a href="/watch-listen" class="read-more-btn">View All Videos</a> ›
      </div>
    </div>
  </div>
</section>

<section class="calendar">
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
        <h2>Upcoming Highlights</h2>
        <ul class="list-unstyled">
          <% future_events(data.events).first(3).each do |event| %>
            <li class="mb-2">
              <strong><%= event.date.strftime("%-d %B %Y") %></strong><br>
              <%= event.title %> — <%= event.location %>
            </li>
          <% end %>
        </ul>
        <a href="/schedule" class="read-more-btn">Full Schedule</a> ›
      </div>
    </div>
  </div>
</section>
